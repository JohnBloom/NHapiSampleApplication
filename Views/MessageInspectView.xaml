<UserControl x:Class="NHapiSampleApplication.Views.MessageInspectView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <TextBlock Height="27" HorizontalAlignment="Left" Name="fName" Text="{Binding Patient.FirstName}" VerticalAlignment="Top" Width="300" />
        <TextBlock Height="26" HorizontalAlignment="Left" Name="mName" Text="{Binding Patient.MiddleName}" VerticalAlignment="Top" Width="300" Margin="0,33,0,0" />
        <TextBlock Height="82" HorizontalAlignment="Left" Name="lName" Text="{Binding Patient.LastName}" VerticalAlignment="Top" Width="300" Margin="0,65,0,0" />
    </Grid>
</UserControl>
